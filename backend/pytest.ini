[pytest]
testpaths = tests
asyncio_mode = auto
# Muestra los nombres de los tests que pasan, no solo los que fallan
addopts = -v
# No lanza warnings de deprecación de librerías externas
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
